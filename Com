<template>
  <div class="popup">
    <div class="popup-content">
      <h4>Detailed Component Info</h4>
      <p v-if="component.product"><strong>{{ component.product }}:{{ component.version }}</strong> ({{ component.type }})</p>
      <p v-else-if="component.vulnId"><strong>Vulnerability: {{ component.vulnId }}</strong> ({{ component.severity }})</p>

      <table>
        <tr><td>Additional Info:</td><td>os-pkgs (example)</td></tr>
      </table>

      <button @click="$emit('close')">Close</button>
    </div>
  </div>
</template>

<script setup>
defineProps(['component'])
</script>

<style scoped>
.popup {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(0, 0, 0, 0.5);
}
.popup-content {
  background: white;
  padding: 20px;
  margin: 100px auto;
  width: 300px;
  border-radius: 8px;
}
</style>
